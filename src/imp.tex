\documentclass[../main.tex]{subfiles}

\begin{document}

Tomamos prestado el lenguaje \texttt{IMP} de~\cite{winskel1993formal}.
Es un pequeño lenguaje \enquote{imperativo}, en el sentido de que tiene variables, que 
conservan el \emph{estado} del programa, y \emph{comandos} que modifican
este estado.

Presentamos un ejemplo de programa en \texttt{IMP} que calcula el factorial de un número \texttt{n}:

\begin{minted}{c}
n = 5; i = 2; res = 1;

while i <= n loop
    res = res * i;
    i = i + 1
end
\end{minted}

Utilizaremos Lean como nuestro \enquote{metalenguaje} para definir la sintaxis y la semántica
de \texttt{IMP}.

\section{Sintaxis}

Para definir la sintaxis de \texttt{IMP} utilizaremos los tipos inductivos de Lean,
de este modo obtendremos primero el \enquote{árbol de sintaxis abstracta} del lenguaje.
Distinguimos entre expresiones aritméticas, expresiones booleanas y comandos.
La distinción intuitiva entre expresiones y comandos es que las primeras
\enquote{devuelven} un valor, mientras que los segundos \enquote{modifican} el estado.
Veremos que en realidad, esto no es del todo cierto.

\subsection{Árbol de sintáxis abstracta}

Comenzamos por la expresiones aritméticas, solo dependen de \texttt{Int} y \texttt{String}.

\begin{minted}{lean4}
inductive Aexp where
  | val₁ : Int → Aexp
  | var₁ : String → Aexp
  -- arithmetic
  | add₁ : Aexp → Aexp → Aexp
  | sub₁ : Aexp → Aexp → Aexp
  | mul₁ : Aexp → Aexp → Aexp
\end{minted}

Podemos interpretar la anterior definición como sigue, el tipo \texttt{Aexp} tiene
cinco constructores, a los que Intuitivamente asociamos los siguientes significados:
\begin{itemize}
  \item [\texttt{val₁}:] construye la expresión formada por una única constante entera.
  \item [\texttt{var₁}:] construye la expresión formada por una única variable.
  \item [\texttt{add₁}:] construye la expresión formada por la suma de dos expresiones aritméticas.
  \item [\texttt{sub₁}:] construye la expresión formada por la resta de dos expresiones aritméticas.
  \item [\texttt{mul₁}:] construye la expresión formada por la multiplicación de dos expresiones aritméticas.
\end{itemize}

En realidad el \enquote{constructor de suma} \texttt{add} solo es \enquote{de suma} en
el nombre, ya que hasta que no definamos una semántica para las expresiones
aritméticas lo único que expresa este es que se puede formar una
nueva expresión a partir de dos expresiones ya existentes.

Lo mismo podemos decir del constructor \texttt{var}, que nos permitirá
\enquote{acceder} al contenido de una variable. En realidad, sintacticamente,
solo nos permite \enquote{construir} una expresión aritmética a partir de un \texttt{String}.

Por ejemplo podemos representar la expresión aritmética \(x + 3\) como 
\begin{minted}{lean4}
#check Aexp.add (Aexp.var "x") (Aexp.val 3)
\end{minted}

O mejor aún usar las coerciones de tipos incluidas en el \enquote{núcleo} de Lean para
obtener los abusos de notación habituales:

\begin{minted}{lean4}
instance: OfNat Aexp n := ⟨Aexp.val₁ n⟩
instance: Add Aexp := ⟨Aexp.add₁⟩
instance: Sub Aexp := ⟨Aexp.sub₁⟩
instance: Neg Aexp := ⟨λ a => Aexp.sub₁ 0 a⟩
instance: Mul Aexp := ⟨Aexp.mul₁⟩

-- x + 3
#check Aexp.var₁ "x" + 3

-- x * -3
#check Aexp.var₁ "x" * -3
\end{minted}

Podríamos incluso implementar una coerción de \texttt{String} a \texttt{Aexp.var₁},
aunque en este caso Lean no sería capaz de inferir el tipo de las expresiones.

Seguimos con las expresiones booleanas.

\begin{minted}{lean4}
inductive Bexp where
  -- constants
  | true₁
  | false₁
  -- boolean
  | not₁ : Bexp → Bexp
  | and₁ : Bexp → Bexp → Bexp
  | or₁  : Bexp → Bexp → Bexp
  -- comparison
  | eq₁ : Aexp → Aexp → Bexp
  | le₁ : Aexp → Aexp → Bexp
\end{minted}

Tenemos los siguientes constructores:
\begin{itemize}
  \item [\texttt{true₁}:] representa el valor de verdad \(\top\).
  \item [\texttt{false₁}:] representa el valor de verdad \(\bot\).
  \item [\texttt{not₁}:] construye la expresión booleana formada por la negación de otra expresión booleana.
  \item [\texttt{and₁}:] construye la expresión booleana formada por la conjunción de dos expresiones booleanas.
  \item [\texttt{or₁}:] construye la expresión booleana formada por la disyunción de dos expresiones booleanas.
  \item [\texttt{eq₁}:] construye la expresión booleana formada por la comparación de igualdad entre dos expresiones aritméticas.
  \item [\texttt{le₁}:] construye la expresión booleana formada por la comparación de menor o igual entre dos expresiones aritméticas.
\end{itemize}

Por ejemplo, podemos representar la expresión booleana \(!(x \le 3)\) como
\begin{minted}{lean4}
#check Bexp.not₁ (Bexp.le₁ (Aexp.var₁ "x") 3)
\end{minted}

En este caso Lean también cuenta con los operadores de complemento, conjunción y
disjunción.

\begin{minted}{lean4}
instance: Complement Bexp := ⟨Bexp.not₁⟩
instance: AndOp Bexp := ⟨Bexp.and₁⟩
instance: OrOp Bexp := ⟨Bexp.or₁⟩

-- !(x <= 3) || false
#check ~~~(Bexp.le₁ (Aexp.var₁ "x") 3) ||| false₁
\end{minted}

Pasamos al tipo más complicado, los comandos. Esto se debe a que todo programa 
de \text{IMP} tendrá tipo \texttt{Com}, en efecto, todos los programas son una 
concatenación de comandos.

\begin{minted}{lean4}
inductive Com where
  | skip₁
  | cat₁   : Com → Com → Com
  | ass₁   : String → Aexp → Com
  | if₁    : Bexp → Com → Com → Com
  | while₁ : Bexp → Com → Com
\end{minted}

Tenemos los constructores:
\begin{itemize}
  \item[\texttt{skip₁}:] construye el programa vacío.
  \item[\texttt{cat₁}:] concatena comandos para formar uno nuevo.
  \item[\texttt{ass₁}:] corresponde a la asignación de expresiones a variables.
  \item[\texttt{if₁}:] corresponde al constructo de flujo de control \texttt{if}.
  \item{\texttt{while₁}:} corresponde al constructo de flujo de control \texttt{while}.
\end{itemize}

Igual que antes, nuestro \texttt{cat₁} es una instancia de la concatenación
genérica:

\begin{minted}{lean4}
instance: Append Com := ⟨Com.cat₁⟩
\end{minted}

Introducimos también las siguientes notaciones para el \texttt{if₁} y el
\texttt{while₁}:

\begin{minted}{lean4}
notation "if " c " then " a " else " b " end" => Com.if₁ c a b
notation "while " c " loop " a " end" => Com.while₁ c a
\end{minted}

Esto nos permitirá usar las notaciones del lenguaje en el propio Lean.

\subsection{Parser}

Lean nos permite además crear un \enquote{parser} para \texttt{IMP}, gracias
a su sistema de macros. Primero declaramos todos los constructos sintácticos:

\begin{minted}{lean4}
declare_syntax_cat imp

-- general
syntax "(" imp ")" : imp
-- aexp
syntax num: imp
syntax ident: imp
syntax:55 imp:55 "*" imp:56 : imp
syntax:60 imp:60 "+" imp:61 : imp
syntax:60 imp:60 "-" imp:61 : imp
-- bexp
syntax:65 imp:65 "&&" imp:66 : imp
syntax:65 imp:65 "||" imp:66 : imp
syntax:70 imp:70 "==" imp:71 : imp
syntax:70 imp:70 "<=" imp:71 : imp
syntax:80 "!" imp:81 : imp
-- com
syntax:40 imp:40 ";" imp:41 : imp
syntax:50 imp:50 "=" imp:51 : imp
syntax "if" imp "then" imp "else" imp " end" : imp
syntax "while" imp "loop" imp " end" : imp
-- meta
syntax "```imp" imp "```" : term
\end{minted}

Ahora podemos asociar cada uno de estos constructos con su constructor en el
AST:

\begin{minted}{lean4}
macro_rules
  -- keywords
  | `(```imp skip```) => `(Com.skip₁)
  | `(```imp true```) => `(Bexp.true₁)
  | `(```imp false```) => `(Bexp.false₁)
  -- general
  | `(```imp ($x)```) => `(```imp $x```)
  -- aexp
  | `(```imp $x:ident```) => `(Aexp.var₁ $(Lean.quote (toString x.getId)))
  | `(```imp $n:num```)   => `($n)
  | `(```imp $x + $y```)  => `(```imp $x``` + ```imp $y```)
  | `(```imp $x - $y```)  => `(```imp $x``` - ```imp $y```)
  | `(```imp $x * $y```)  => `(```imp $x``` * ```imp $y```)
  -- bexp
  | `(```imp !$x```)      => `(~~~```imp $x```)
  | `(```imp $x && $y```) => `(```imp $x``` &&& ```imp $y```)
  | `(```imp $x || $y```) => `(```imp $x``` ||| ```imp $y```)
  | `(```imp $x == $y```) => `(Bexp.eq₁ ```imp $x``` ```imp $y```)
  | `(```imp $x <= $y```) => `(Bexp.le₁ ```imp $x``` ```imp $y```)
  -- com
  | `(```imp $x:ident = $y```) => `(Com.ass₁ $(Lean.quote (toString x.getId)) ```imp $y```)
  | `(```imp $x ; $y```)       => `(Com.cat₁ ```imp $x``` ```imp $y```)
  | `(```imp if $b then $x else $y end```) => `(Com.if₁ ```imp $b``` ```imp $x``` ```imp $y```)
  | `(```imp while $b loop $x end```) => `(Com.while₁ ```imp $b``` ```imp $x```)
\end{minted}

Con esto podemos introducir en Lean el ejemplo con el que hemos empezado el
capítulo:

\begin{minted}{lean4}
#check
```imp
  n = 5; i = 2; res = 1;

  while i <= n loop
      res = res * i;
      i = i + 1
  end
```
\end{minted}

\section{Estados}

Antes de poder darle significado a estos constructos sintácticos, necesitamos
\emph{estados} que guarden el valor de las variables del programa. Un estado
no es más que un mapa de \texttt{String} a \texttt{Int}. Lo definiremos
de una forma que nos permita demostrar facilmente algunas propiedades deseables.
Definimos un \emph{Mapa Total} genérico, por simplicidad, todas las variables
de nuestro lenguaje estarán inicializadas a 0.

\begin{minted}{lean4}
def TotalMap A := String → A

def TotalMap.default (v: A): TotalMap A := λ _ => v

def TotalMap.update (m: TotalMap A) (k: String) (v: A) :=
  λ k' => bif k == k' then v else m k'

notation m "[" k " ← " v "]" => TotalMap.update m k v
\end{minted}

Como se puede ver un mapa total no es más que una función de \texttt{String} a \texttt{A},
donde \texttt{A} es el tipo de los valores que queremos guardar. El truco para
definir el mapa está en la función \texttt{update}, que nos permite modificar
el \enquote{contenido} de una variable.

Nos detenemos un momento a explicar lo que es la función \texttt{bif}, 
es simplemente el \texttt{if} tradicional para valores booleanos, en vez
de para proposiciones, esto es, la condición toma un valor booleano decidible.
Puesto que la comparación de strings es decidible, podemos usarla en la definición.
Podríamos haber hecho lo mismo con el \texttt{if} tradicional, pero esto permite
a Lean inferir mejor los tipos.

Introducimos también la notación \texttt{m[k ← v]} que indica que se ha
asignado a la variable \texttt{k} el valor \texttt{v} en el mapa \texttt{m}.
Esto nos permite concatenar asignaciones:

\begin{minted}{lean4}
m[k ← v₂][k ← v₁]
\end{minted}

Indica que primero se asigna a \texttt{k} el valor \texttt{v₂} y luego el valor \texttt{v₁}.
Uno podría esperar que esto sea equivalente a \texttt{m[k ← v₁]}, y así es,
como demostraremos en el siguiente lema.

\begin{minted}{lean4}
theorem eval_last:
  m[k ← v₂][k ← v₁] = m[k ← v₁] := by
  funext k'
  match h: k == k' with
  | true =>
    unfold TotalMap.update
    rw [h, cond_true, cond_true]
  | false =>
    unfold TotalMap.update
    rw [h, cond_false, cond_false, cond_false]
\end{minted}

Otra propiedad razonable es que no importa el orden en el que se hagan las asignaciones
a dos variables distintas.

\begin{minted}{lean4}
theorem eval_swap (hneq : k₁ != k₂):
  m[k₂ ← v₂][k₁ ← v₁] = m[k₁ ← v₁][k₂ ← v₂] := by
  funext k'
  match h: k₁ == k' with
  | true =>
    unfold TotalMap.update
    rw [h, cond_true]
    match h': k₂ == k' with
    | true =>
      rw [eq_of_beq h, eq_of_beq h', bne_iff_ne] at hneq
      contradiction
    | false =>
      rw [cond_false, cond_true]
  | false =>
    unfold TotalMap.update
    rw [h, cond_false, cond_false]
\end{minted}

Definimos ahora las \emph{configuraciones} que son pares formados por un 
\enquote{constructo} y un estado, para cada uno
de los tipos que acabamos de definir tendremos un tipo de configuración.

\begin{minted}{lean4}
def AConfig := Aexp × State
def BConfig := Bexp × State
def Config := Com × State
\end{minted}

La importancia de las configuraciones radica en que dado un constructo y un estado
podremos \enquote{evaluar} el constructo en el estado, obteniendo un nuevo estado.
Esto es lo que estudiaremos cuando veamos la \emph{semántica} de \texttt{IMP}.

\section{Semántica de \texttt{Aexp}}

Comenzamos definiendo la semántica de las expresiones aritméticas, partiendo por
la de \emph{paso largo} o \emph{natural}.



\end{document}